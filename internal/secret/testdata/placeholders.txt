# .env.example — a template checked into the repo on purpose.
# None of these are real; the tripwire must stay quiet.
API_KEY=changeme
SECRET_TOKEN=<your-token>
DATABASE_PASSWORD=${DB_PASSWORD}
CLIENT_SECRET=your-key
AUTH_TOKEN=xxxxxxxxxxxx
SESSION_KEY=example
